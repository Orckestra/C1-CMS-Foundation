/**
* Composite image resize plugin.
*/
tinymce.PluginManager.add('composite', function (editor) {

	editor.on('KeyDown', function (e) {
		if (e.keyCode == 13) {
			if (editor.theme.handleEnterKey) {
				editor.theme.handleEnterKey(e);
			}
		}
	});
});